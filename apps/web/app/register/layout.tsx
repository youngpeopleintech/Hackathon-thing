import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Register — The Artificial Future | YPIT AF 2026',
  description: 'Register for the YPIT AF hackathon, workshops, and conference in Lagos.',
};

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
